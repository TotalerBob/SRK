import React from "react";

export const DonateButton = ({ onClick }) => (
  <button
    className="donate-button"
    value="Bester Button EUW"
    onClick={() => this.props.history.push("/donate")}
  />
);
